[project]
name = "compass"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "prem-v", email = "premvy007@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
compass = "compass:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
